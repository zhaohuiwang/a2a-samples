[project]
name = "ag2-a2a"
version = "0.1.0"
description = "MCP Mypy agent using A2A and AG2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ag2[mcp,openai,a2a]>=0.10.0",
    "mypy>=1.10.0",
]
